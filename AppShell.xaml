<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:QuickCol.Views"
       xmlns:viewr="clr-namespace:QuickCol.Views.Register"
       xmlns:local="clr-namespace:QuickCol"  

    x:Class="QuickCol.AppShell">

    <!-- Définition des routes pour la navigation -->
    <ShellContent Route="MainPage" ContentTemplate="{DataTemplate local:MainPage}" />

    <ShellContent Route="LoginPage" ContentTemplate="{DataTemplate views:LoginPage}" />
    <ShellContent Route="RegisterStep1Page" ContentTemplate="{DataTemplate viewr:RegisterStep1Page}" />
    <ShellContent Route="RegisterStep2Page" ContentTemplate="{DataTemplate viewr:RegisterStep2Page}" />
    <ShellContent Route="RegisterStep3Page" ContentTemplate="{DataTemplate viewr:RegisterStep3Page}" />

</Shell>

